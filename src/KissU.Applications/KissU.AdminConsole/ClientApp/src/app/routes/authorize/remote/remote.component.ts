import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'authorize-remote',
  templateUrl: './remote.component.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class AuthorizeRemoteComponent {}
